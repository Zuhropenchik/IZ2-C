set(LIBRARY_NAME Sorting_Consistent)
file(GLOB SRC Sorting.c ../Sorting.h)
add_library(${LIBRARY_NAME} STATIC ${SRC})
target_include_directories(${LIBRARY_NAME} PUBLIC ${CMAKE_CURRENT_SOURCE_DIR} ../.)
target_link_libraries(${LIBRARY_NAME} LINK_PUBLIC Letters Vector)